name: yemen_project
description: Flex Yemen - The ultimate marketplace for Yemen.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  geolocator: ^12.0.0
  flutter_local_notifications: ^16.3.2
  image_picker: ^1.0.7
  share_plus: ^7.2.1
  google_maps_flutter: ^2.5.3
  share_plus: ^7.2.1
  firebase_messaging: ^14.7.10
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  supabase_flutter: ^2.0.2
  geolocator: ^10.1.0
  url_launcher: ^6.2.1

dev_dependencies:
  geolocator: ^12.0.0
  flutter_local_notifications: ^16.3.2
  image_picker: ^1.0.7
  share_plus: ^7.2.1
  google_maps_flutter: ^2.5.3
  share_plus: ^7.2.1
  firebase_messaging: ^14.7.10
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

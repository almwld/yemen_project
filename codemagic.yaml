workflows:
  android-workflow:
    name: Yemen Market Termux Build
    environment:
      flutter: stable
      java: 17
    scripts:
      - name: Fix Build
        script: |
          flutter pub get
          flutter build apk --release --android-skip-build-dependency-validation
    artifacts:
      - build/app/outputs/flutter-apk/**/app-release.apk

workflows:
  android-workflow:
    name: Production Build
    environment:
      flutter: stable
    scripts:
      - name: Build
        script: |
          flutter clean
          flutter pub get
          flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
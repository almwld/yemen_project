<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    >
    <uses-permission android:name=".MainActivity" />
   <application
        android:label="Flex Yemen Market"
        android:name=".MainActivity"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|density|keyboardHidden|keyboard"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name=".MainActivity"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name=".MainActivity"/>
                <category android:name=".MainActivity"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name=".MainActivity"
            android:value="2" />
    </application>
</manifest>
